<div class="pics" id="box-slider-one">
    <!-- Images -->
    {% for book in res %}
        <a href="{{ path('detail', {bookid: book.bookid}) }}"><img src="{{ book.img }}" class="img-responsive" alt="{{ book.title }}" /></a>
    {% endfor %}
</div>