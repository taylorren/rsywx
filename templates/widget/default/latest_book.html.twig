<div class="box-content box-gallery padd-zero">
    <div class="img-details box-hover-black">
        <!-- Heading -->
        <h3>最新收藏的书</h3>
        <!-- Paragraph -->
        <p>最新收藏于{{ res.purchdate|date("Y年m月d日") }}的书是{{ res.author }}所著的《<a href="{{ path("detail", {bookid:res.bookid}) }}" class="blue">{{ res.title }}</a>》。</p>
        <p><strong>摘要：</strong><br>{{ res.intro }}</p>
        <!-- View Next Button -->
        <div class="view-button">
            <a href="{{ path("detail", {bookid:res.bookid}) }}" class="btn btn-danger">更多内容 <i class="fas fa-angle-right"></i></a>
        </div>
    </div>
    <!-- Image -->
    <img src="{% if api=='prod' %}https://api.rsywx.com/book/image/{% else %}http://api/book/image/{% endif %}{{ res.bookid  }}/{{ res.author }}/{{ res.title }}/600" class="img-responsive box-gallery-img" alt="{{ res.title }}" />
    
</div>
